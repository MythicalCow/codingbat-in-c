#include <stdio.h>
#include <string.h>
#define TRUE 1
#define FALSE 0

int sleepIn(int talking, int hour){
    return (talking || hour < 7) && (talking || hour > 20);
}
//char takes up 1 byte
int main() {
    printf("Parrot Trouble: ", 
    return 0;
}
